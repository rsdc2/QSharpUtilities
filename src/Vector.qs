namespace Utlities.Vector {
    import Std.Math.*;
    import Std.Arrays.*;
    import Utilities.Complex.*;

    function AreEqualVectorsC(A : Complex[], B: Complex[]) : Bool {
        let applics = Mapped(EqualC(_, _))
    } 
}